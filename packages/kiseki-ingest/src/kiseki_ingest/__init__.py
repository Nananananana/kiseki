"""Reference implementation that turns EXIF metadata into PhotoRecord documents.

This package is not part of the core. It exists to show what a producer looks
like and to give the project a working input path. An import-linter contract
forbids it from importing the core domain, so a producer written in Swift or
Kotlin stands on exactly the same footing.
"""

__version__ = "0.1.0"

[project]
name = "kiseki-workspace"
version = "0.0.0"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
kiseki = { workspace = true }
kiseki-ingest = { workspace = true }
kiseki-conformance = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-cov",
    "ruff",
    "mypy",
    "import-linter",
    "pre-commit",
    "jsonschema",
    "pillow",
    "piexif",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
mypy_path = "packages/kiseki-core/src:packages/kiseki-ingest/src:packages/kiseki-conformance/src"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "llm: 実 LLM を呼ぶテスト。CI では実行しない",
    "slow: 実行に時間がかかるテスト",
]
addopts = "-m 'not llm'"